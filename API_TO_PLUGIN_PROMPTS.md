# API-to-plugin prompt

The previous multi-file prompt pack targeted the retired fragmented plugin format.

Use the canonical schema 1.0 prompt in [PROMPT_GENERADOR_PLUGIN_API.md](PROMPT_GENERADOR_PLUGIN_API.md). It produces one self-contained `plugin.json`, a validation checklist and the CLI commands needed to run it.
